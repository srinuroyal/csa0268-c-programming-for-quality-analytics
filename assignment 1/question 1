#include<stdio.h>

int main()
{
    int num;
     printf("enter the num value")
    scanf("%d",&num);
 
    if(num == 0)
        printf("");
    else if(num < 0)
        printf("Negative");
    else
        printf("Positive");
     
    return 0;
}
